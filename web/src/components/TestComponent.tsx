import Discover from "./Discover"
export default function TestingComponent() {
    return (
        <div className="dashboard"> {/*try add className="dashboard" inside the div tag */}
            <Discover/>
        </div>
    )
}